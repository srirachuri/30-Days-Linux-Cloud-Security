# Day 19: Archiving & Compression (Tar/Gzip) 🗜️

## 🎯 Goal
Learn to bundle and compress files using `tar` and `gzip`, essential for creating backups and transferring data.

## 🧩 Commands Learned
| Command | Purpose | Example |
| :--- | :--- | :--- |
| `tar -cvf` | Create Archive | `tar -cvf backup.tar /data` |
| `gzip` | Compress | `gzip file.txt` |
| `tar -xvf` | Extract | `tar -xvf backup.tar` |
| `zip` / `unzip` | Zip Format | Standard zip files |

## 🛠️ Practice Session
I created a directory of logs, bundled them into a `.tar` archive, and then compressed it into a `.tar.gz` file to save space.

---

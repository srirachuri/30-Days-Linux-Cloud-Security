# Day 21: Archiving and Backups (Tar & Zip) 📦

## 🎯 Goal
Learn how to compress and bundle files using `tar`, `zip`, and `gzip` to save space and create backups.

## 🧩 Commands Learned
| Command | Purpose | Example |
| :--- | :--- | :--- |
| `tar -cvf` | Create Archive | `tar -cvf backup.tar /folder` |
| `tar -xvf` | Extract Archive | `tar -xvf backup.tar` |
| `gzip` | Compress File | `gzip file.txt` |
| `zip` | Create Zip | `zip archive.zip file1 file2` |
| `unzip` | Extract Zip | `unzip archive.zip` |

## 🛠️ Practice Session
I created a workspace `project21`, created multiple dummy files, and practiced creating both `.tar` archives and `.zip` files.

---
